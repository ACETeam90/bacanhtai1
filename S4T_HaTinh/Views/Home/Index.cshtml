@{
    var session = Session["User"];
    if (session == null) { 
        //Layout = null;
        Layout = "~/Views/Shared/_Layout_Null.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }        
}

<h3>PHẦN MỀM QUẢN LÝ VỀ CNTT CỦA PHÒNG CNTT SỞ THÔNG TIN TRUYỀN THÔNG</h3>

@*@using Microsoft.AspNet.Identity
    @if (Request.IsAuthenticated)
    {
        using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
        {
        @Html.AntiForgeryToken()

        <ul class="nav navbar-nav navbar-right">
            <li>
                @Html.ActionLink("Hello " + User.Identity.GetUserName() + "!", "Index", "Manage", routeValues: null, htmlAttributes: new { title = "Manage" })
            </li>
            <li><a href="javascript:document.getElementById('logoutForm').submit()">Log off</a></li>
        </ul>
        }
    }
    else
    {
        <ul class="nav navbar-nav navbar-right">
            <li>@Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
        </ul>
    }*@
<div class="row">
    @*<input id="sessionInput" type="hidden" value="@Session["User"]" />*@
    @*<input id="btnPopup" type="button" value="popup" />*@
</div>
<div id="dialog" style="display: none"> </div>
<script type="text/javascript">
    $(document).ready(function () {
        var session = '@Session["User"]';
        if (session == '') {
            //$("#dialog").dialog({
            //    title: "Đăng nhập",
            //    modal:true,
            //    width: 420,
            //    //height:430,
            //    buttons: {
            //        Close: function () {
            //            $(this).dialog('close');
            //            //$("#dialog").dialog("open");
            //        }
            //    }
            //});
            //$("#dialog").load('/Account/login2', function () {
            //    $("#dialog").dialog("open");
            //});
            window.location.href = '/Account/Login';
        }
    });

    $("[id*=btnPopup]").click(function () {
        $("#dialog").dialog({
            title: "Đăng nhập",
            modal: true,
            width: 420,
            //height: 430,
            buttons: {
                Close: function () {
                    $(this).dialog('close');
                }
            }
        });
        $("#dialog").load('/Account/login', function () {
            $("#dialog").dialog("open");
        });
    });
</script>  